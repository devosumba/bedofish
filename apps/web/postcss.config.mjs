/**
 * BEDO FISH — CRITICAL PostCSS config.
 * Must use @tailwindcss/postcss for Tailwind v4.
 * Deleting or changing this breaks all site styling.
 */
export default {
  plugins: { '@tailwindcss/postcss': {} },
}
